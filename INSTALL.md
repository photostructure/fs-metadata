## Prerequisite dev packages

On Windows:

    choco install llvm

On macOS:

    brew install clang-format

On Ubuntu/Debian:

    sudo apt-get install build-essential clang-format libglib2.0-dev libblkid-dev uuid-dev
